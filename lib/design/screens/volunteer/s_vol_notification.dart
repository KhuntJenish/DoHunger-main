// TODO:// Jenish Volunteernotification screen

import 'package:flutter/material.dart';

class SVolNotification extends StatelessWidget {
  static const routeName = '/s-vol-notification';
  const SVolNotification({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
